// my written  code 
// Math methods
Math.abs(-5)//5
Math.abs(2)//2
Math.floor(2.222)//2
Math.ceil(2.22)//3
Math.round(2.22)//2
Math.round(2.65)//3
Math.random()//any random number btw 0 to1 like 0.3232442335

// Math.random methods
// first method
const firstMethod = Math.floor((Math.random() * /* n */10) + 1)//here can be any number on where i want how much maximum it number can go
//second method
let min = 10
let max = 20
let secondMethod = Math.floor((Math.random() * (max - min + 1)) + min)

console.log(firstMethod)
console.log(secondMethod)




// Ai written code
// Math methods

// Math.abs() returns the absolute value of a number
Math.abs(-5); // 5
Math.abs(3.14); // 3.14

// Math.ceil() rounds a number up to the nearest integer
Math.ceil(4.4); // 5
Math.ceil(4.7); // 5

// Math.floor() rounds a number down to the nearest integer
Math.floor(4.4); // 4
Math.floor(4.7); // 4

// Math.max() returns the largest value from a list of numbers
Math.max(1, 3, 2); // 3
Math.max(-1, -3); // -1

// Math.min() returns the smallest value from a list of numbers
Math.min(1, 3, 2); // 1
Math.min(-1, -3); // -3

// Math.pow() raises a number to a power
Math.pow(2, 3); // 8 (2 * 2 * 2)
Math.pow(4, 0.5); // 2 (square root of 4)

// Math.random() returns a random number between 0 and 1
Math.random(); // e.g. 0.7495837188629236

// Math.round() rounds a number to the nearest integer
Math.round(4.4); // 4
Math.round(4.7); // 5

// Math.sqrt() returns the square root of a number
Math.sqrt(9); // 3
Math.sqrt(2); // 1.4142135623730951


